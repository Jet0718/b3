<?xml version="1.0"?>
<odoo>
    <record id="estate_property_offer_view_tree" model="ir.ui.view">
        <field name="name">estate.property.offer.tree</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree string="Estate Property Offer" editable='top' 
            decoration-success="status=='accepted'" 
            decoration-danger="status=='refused'">
                <field name="price" string="Price" />
                <field name="partner_id" string="Partner" />
                <field name="validity" string="Validity(days)"/>
                <field name="date_deadline" string="Deadline"/>
                <field name="property_type_id" string="Type id"/>
                <button name="action_accept" title='Accept' class='oe_highlight' type="object" icon="fa-check" invisible="status"/>
                <button name="action_refuse" title='Refuse' class='oe_highlight' type="object" icon="fa-times" invisible="status"/>
                <field name="status"  column_invisible="True"/>
            </tree>
        </field>
    </record>
    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Estate Property Offer">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity" string="Validity(days)"/>
                        <field name="date_deadline" string="Deadline"/>
                        <field name="status"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>